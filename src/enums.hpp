#pragma once

enum class Weekday {
	Monday,
	Tuesday,
	Wednesday,
	Thursday,
	Friday,
	Saturday,
	Sunday,
};

enum class employeeType {
	worker,
	ticketSeller,
	foodSeller,
	janitor,
};

enum class WorkplaceType {
	ticketCounter,
	janitorialCloset,
	foodCounter,
};
